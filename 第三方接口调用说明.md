# HZCYKJTHardWare 第三方接口调用说明

DLL 文件名：`HZCYKJTHardWare.dll`

目标平台：32 位 Win32/x86
调用约定：`__stdcall`
函数返回值：`1` 表示成功或请求已提交，`0` 表示失败。

## 1. 文件部署

第三方程序运行目录需要放置：

- `HZCYKJTHardWare.dll`
- `HZCYKJTHardWare.exe`
- `HZCYKJTHardWare.json`

DLL 初始化时自动读取同目录下的 `HZCYKJTHardWare.json`。默认配置下，如果 Delphi 后端程序尚未运行，`InitSdk` 会自动启动同目录的 `HZCYKJTHardWare.exe` 并等待 `/ping` 可用。如果同路径后端进程存在但通信服务在初始化开始后的 8 秒内仍不可用，DLL 会记录异常并自动重启该后端进程。第三方接口不需要传终端 IP，终端地址由配置文件和 `HZCYKJTHardWare_SwitchTerminal` 共同决定。

### 1.1 架构说明

```
第三方程序 → DLL → delphi_server.host:port → Delphi后端程序 → 终端设备(terminal.port)
                              ← DLL回调(JSON字符串) ← 解析结果 ← 终端回调
```

DLL 不直接调用终端。所有硬件通信由 Delphi 后端程序负责。通过 `delphi_server.auto_start` 可控制是否由 DLL 在初始化时自动启动后端程序。

### 1.2 端口配置

- `delphi_server.port`：Delphi 后端监听、DLL 请求，默认 `8080`。
- `terminal_callback_server.port`：Delphi 后端监听、终端回调，默认 `8081`。
- `callback_server.port`：DLL 监听、Delphi 后端回调，默认 `39091`。
- `terminal.port`：Delphi 后端请求终端设备的目标端口，默认 `9098`，不是本机监听端口。

### 1.3 日志

- DLL 日志：`HZCYKJTHardWareDLL_Logs\HZCYKJTHardWareDLL_Logs_yyyyMMdd.log`
- 后端程序日志：`HZCYKJTHardWareExe_Logs\HZCYKJTHardWareExe_Logs_yyyyMMdd.log`
- DLL 配置了其他 `log.dir` 时，日志文件名使用该目录最后一级名称加日期，例如 `DeviceDllLogs\DeviceDllLogs_20260526.log`。

## 2. 推荐调用流程

```c
HZCYKJTHardWare_InitSdk();
HZCYKJTHardWare_RegisterEventCallback(OnEvent);

HZCYKJTHardWare_SwitchTerminal(1);
HZCYKJTHardWare_StartProcess(".\\captures");

HZCYKJTHardWare_StartCameraPreview(hwndCamera);
HZCYKJTHardWare_StartFingerprintPreview(hwndFingerprint);

HZCYKJTHardWare_CaptureCameraImage(".\\captures\\camera_001.jpg");
HZCYKJTHardWare_CaptureFingerprintImage(".\\captures\\fingerprint_001.jpg");
HZCYKJTHardWare_RequestOCR(".\\captures");
HZCYKJTHardWare_RequestNfcCard(".\\captures");

HZCYKJTHardWare_StopCameraPreview();
HZCYKJTHardWare_EndProcess();
HZCYKJTHardWare_ReleaseSdk();
```

说明：
- `InitSdk` 必须先调用。
- `RegisterEventCallback` 必须调用（异步结果通过回调通知）。
- `StartProcess` / `EndProcess` 标记业务流程起止。
- `SwitchTerminal` 切换终端，同终端不会重复切换。
- `ReleaseSdk` 释放所有资源。

## 3. 事件回调（新版：JSON 字符串）

### 3.1 回调签名

```c
typedef void (__stdcall *THZCYKJTHardWareEventCallback)(const char* eventJson);
```

**与旧版区别：** 不再使用 struct + userData，改为直接传 JSON 字符串。第三方无需 include 头文件，解析 JSON 即可。

### 3.2 注册回调

```c
int __stdcall HZCYKJTHardWare_RegisterEventCallback(
    THZCYKJTHardWareEventCallback callback
);
```

### 3.3 回调 JSON 格式

```json
{
  "event_type": 1601,
  "request_id": "HZCYKJTHardWare_OCR_...",
  "resource_type": "ocr_document",
  "status": 1,
  "error_code": "",
  "message": "OCR completed successfully",
  "terminal_index": 1,
  "terminal_base_url": "http://192.168.20.30:9098",
  "save_path": ".\\captures\\OCR_xxx.json",
  "mrz": "MRZ1^MRZ2^MRZ3",
  "ic_number": "card_text_value"
}
```

字段说明：

| 字段 | 类型 | 说明 |
|------|------|------|
| `event_type` | int | 事件类型编号 |
| `request_id` | string | 本次请求唯一标识 |
| `resource_type` | string | 资源类型 |
| `status` | int | 1=成功, 负数=失败 |
| `error_code` | string | 失败时的错误码 |
| `message` | string | 人类可读描述 |
| `terminal_index` | int | 当前终端序号 |
| `terminal_base_url` | string | 当前终端地址 |
| `save_path` | string | 保存路径（如有） |
| `mrz` | string | OCR 机读码，格式 `MRZ1^MRZ2^MRZ3`（仅 OCR 事件） |
| `ic_number` | string | IC 卡号（仅 NFC 事件） |

### 3.4 如何提取 MRZ

OCR 成功事件 `event_type=1601` 时，`mrz` 字段包含证件机读码：

```c
void __stdcall OnEvent(const char* json) {
    int eventType = GetJsonInt(json, "event_type");
    if (eventType == 1601) {  // OCR 成功
        const char* mrz = GetJsonString(json, "mrz");
        // mrz = "MRZ1^MRZ2^MRZ3"，用 '^' 分割可得三行
        const char* savePath = GetJsonString(json, "save_path");
    }
}
```

### 3.5 如何提取 card_text

NFC/IC 卡成功事件 `event_type=1806` 时，`ic_number` 字段包含卡号：

```c
void __stdcall OnEvent(const char* json) {
    int eventType = GetJsonInt(json, "event_type");
    if (eventType == 1806) {  // NFC 成功
        const char* cardText = GetJsonString(json, "ic_number");
    }
}
```

### 3.6 常用事件类型

| 事件号 | 含义 | 关键字段 |
|--------|------|---------|
| 1003 | 终端已切换 | terminal_index |
| 1201 | 摄像头预览已开始 | — |
| 1202 | 摄像头预览已停止 | — |
| 1203 | 摄像头预览失败 | message |
| 1301 | 指纹预览已开始 | — |
| 1302 | 指纹预览已停止 | — |
| 1303 | 指纹预览失败 | message |
| 1601 | OCR 成功 | **mrz**, save_path |
| 1602 | OCR 失败 | message |
| 1801 | 虹膜预览已开始 | — |
| 1802 | 虹膜预览已停止 | — |
| 1803 | 虹膜预览失败 | message |
| 1804 | 虹膜抓拍成功 | save_path |
| 1805 | 虹膜抓拍失败 | message |
| 1806 | IC 卡识别成功 | **ic_number** |
| 1807 | IC 卡识别失败 | message |
| 1999 | 通用错误 | message |

## 4. 导出函数

### 4.1 初始化 DLL

```c
int __stdcall HZCYKJTHardWare_InitSdk(void);
```

初始化 DLL。成功返回 `1`，失败返回 `0`。失败原因见 DLL 日志。

### 4.2 释放 DLL

```c
int __stdcall HZCYKJTHardWare_ReleaseSdk(void);
```

释放所有资源，并分别停止当前正在运行的预览。

### 4.3 注册事件回调

```c
int __stdcall HZCYKJTHardWare_RegisterEventCallback(
    THZCYKJTHardWareEventCallback callback
);
```

注册 JSON 字符串回调。成功返回 `1`。

### 4.4 开始流程

```c
int __stdcall HZCYKJTHardWare_StartProcess(const char* saveDir);
```

- `saveDir`：本次流程的保存目录，如 `".\\captures"`。可传 `NULL` 使用配置默认值。

### 4.5 结束流程

```c
int __stdcall HZCYKJTHardWare_EndProcess(void);
```

结束流程，取消未完成的异步请求。

### 4.6 切换终端

```c
int __stdcall HZCYKJTHardWare_SwitchTerminal(int terminalIndex);
```

- `terminalIndex`：`1` 或 `2`。
- 同终端不会重复切换，直接返回 `1`。
- 切换后仅恢复切换前正在运行的预览，并继续使用第三方已传入的目标窗口。

### 4.7 摄像头预览

```c
int __stdcall HZCYKJTHardWare_StartCameraPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopCameraPreview(void);
```

`hwnd` 为第三方窗口句柄，必须在调用时有效。libVLC 直接绑定该窗口，并使用原生裁切参数将摄像头源按 `1920x1080` 保持比例、以中心为主体裁切铺满；目标窗口尺寸变化时仅更新裁切参数。

预览启动接口为同步转发调用。如果 VLC 首次连接视频流耗时较长，应将配置文件中的 `timeout.http_request_ms` 设置为足够的等待时间，例如 `15000`；这可避免启动成功前 DLL 先按超时返回失败，但调用线程仍会等待视频启动完成。

### 4.8 指纹预览

```c
int __stdcall HZCYKJTHardWare_StartFingerprintPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopFingerprintPreview(void);
```

指纹源按 `640x640` 保持比例并以中心为主体裁切铺满目标窗口。

### 4.9 虹膜预览

```c
int __stdcall HZCYKJTHardWare_StartIrisPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopIrisPreview(void);
```

### 4.10 人脸抓拍（同步）

```c
int __stdcall HZCYKJTHardWare_CaptureCameraImage(const char* saveDir);
```

同步抓拍。参数保留原函数名，但摄像头和指纹抓拍的路径规则如下：

- 参数包含文件扩展名时，按第三方传入的完整相对或绝对文件路径保存并覆盖同名文件，例如 `".\\captures\\111.jpg"`，不追加程序后缀。
- 参数为 `NULL`、空字符串或仅为目录时，使用 JSON 中的 `save.camera_default_path`。
- 相对路径以后端 EXE 所在目录为基准。

配置示例：

```json
"save": {
  "camera_default_path": ".\\captures\\camera.jpg",
  "fingerprint_default_path": ".\\captures\\fingerprint.jpg"
}
```

### 4.11 指纹抓拍（同步）

```c
int __stdcall HZCYKJTHardWare_CaptureFingerprintImage(const char* saveDir);
```

保存路径判定规则与摄像头抓拍一致；未传具体文件名时使用 `save.fingerprint_default_path`。

### 4.12 虹膜抓拍（异步）

```c
int __stdcall HZCYKJTHardWare_CaptureIrisImage(const char* saveDir);
```

返回 `1` 表示请求已提交，结果通过 `1804`/`1805` 事件回调。

### 4.13 OCR 识别（异步）

```c
int __stdcall HZCYKJTHardWare_RequestOCR(const char* saveDir);
```

返回 `1` 表示请求已提交。刷证件后结果通过 `1601` 事件回调，`mrz` 字段包含机读码。

### 4.14 IC 卡识别（异步）

```c
int __stdcall HZCYKJTHardWare_RequestNfcCard(const char* saveDir);
```

返回 `1` 表示请求已提交。刷卡后结果通过 `1806` 事件回调，`ic_number` 字段包含卡号。

### 4.15 车牌预览（预留）

```c
int __stdcall HZCYKJTHardWare_StartPlatePreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopPlatePreview(void);
```

暂未实现。

## 5. C/C++ 声明示例

```c
// 回调类型
typedef void (__stdcall *THZCYKJTHardWareEventCallback)(const char* eventJson);

// DLL 导入
int __stdcall HZCYKJTHardWare_InitSdk(void);
int __stdcall HZCYKJTHardWare_ReleaseSdk(void);
int __stdcall HZCYKJTHardWare_RegisterEventCallback(THZCYKJTHardWareEventCallback callback);
int __stdcall HZCYKJTHardWare_StartProcess(const char* saveDir);
int __stdcall HZCYKJTHardWare_EndProcess(void);
int __stdcall HZCYKJTHardWare_SwitchTerminal(int terminalIndex);
int __stdcall HZCYKJTHardWare_StartCameraPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopCameraPreview(void);
int __stdcall HZCYKJTHardWare_StartFingerprintPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopFingerprintPreview(void);
int __stdcall HZCYKJTHardWare_StartIrisPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopIrisPreview(void);
int __stdcall HZCYKJTHardWare_CaptureCameraImage(const char* saveDir);
int __stdcall HZCYKJTHardWare_CaptureFingerprintImage(const char* saveDir);
int __stdcall HZCYKJTHardWare_CaptureIrisImage(const char* saveDir);
int __stdcall HZCYKJTHardWare_RequestOCR(const char* saveDir);
int __stdcall HZCYKJTHardWare_RequestNfcCard(const char* saveDir);

// 回调示例
void __stdcall OnEvent(const char* json) {
    // 自己解析 JSON，提取 event_type / mrz / ic_number / save_path
}
```

## 6. Delphi 7 声明示例

```pascal
type
  THZCYKJTHardWareEventCallback = procedure(EventJson: PAnsiChar); stdcall;

const
  DLL_NAME = 'HZCYKJTHardWare.dll';

function HZCYKJTHardWare_InitSdk: Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_ReleaseSdk: Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_RegisterEventCallback(Callback: THZCYKJTHardWareEventCallback): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_StartProcess(SaveDir: PAnsiChar): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_EndProcess: Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_SwitchTerminal(TerminalIndex: Integer): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_StartCameraPreview(Hwnd: Pointer): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_StopCameraPreview: Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_StartFingerprintPreview(Hwnd: Pointer): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_StopFingerprintPreview: Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_StartIrisPreview(Hwnd: Pointer): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_StopIrisPreview: Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_CaptureCameraImage(SaveDir: PAnsiChar): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_CaptureFingerprintImage(SaveDir: PAnsiChar): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_CaptureIrisImage(SaveDir: PAnsiChar): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_RequestOCR(SaveDir: PAnsiChar): Integer; stdcall; external DLL_NAME;
function HZCYKJTHardWare_RequestNfcCard(SaveDir: PAnsiChar): Integer; stdcall; external DLL_NAME;
```

Delphi 回调示例：

```pascal
procedure EventCallback(EventJson: PAnsiChar); stdcall;
var
  Json: string;
begin
  Json := string(EventJson);  // UTF-8 JSON，按需转 ANSI
  // 解析 JSON 提取 event_type / mrz / ic_number / save_path
  // 例如：Pos('"mrz":"', Json) 找到 MRZ 值
end;
```
