# HZCYKJTHardWare 第三方接口调用说明

DLL 文件名：`HZCYKJTHardWare.dll`

目标平台：32 位 Win32/x86  
调用约定：`stdcall`  
函数返回值：`1` 表示成功或请求已提交，`0` 表示失败。

## 1. 文件部署

第三方程序运行目录需要放置：

- `HZCYKJTHardWare.dll`
- `HZCYKJTHardWare.json`
- `vlc` 目录，使用预览功能时必须放置

`HZCYKJTHardWare.json` 由 DLL 初始化时自动读取。第三方接口不需要传终端 IP，终端地址由配置文件和 `HZCYKJTHardWare_SwitchTerminal` 共同决定。

### 1.1 预览低延迟配置

预览使用 DLL 目录下的 `vlc` 运行库播放 RTSP。预览延迟可在 `HZCYKJTHardWare.json` 的 `preview` 节点中调整：

```json
"preview": {
  "renderer": "libvlc",
  "auto_reconnect": true,
  "check_hwnd_interval_ms": 500,
  "stop_preview_on_end_process": false,
  "rtsp_network_caching_ms": 150,
  "rtsp_live_caching_ms": 150,
  "rtsp_transport": "tcp"
}
```

字段说明：

- `rtsp_network_caching_ms`：RTSP 网络缓存时间，单位毫秒。数值越小延迟越低，但网络抖动时更容易卡顿。
- `rtsp_live_caching_ms`：实时流缓存时间，单位毫秒。建议先使用 `150`，现场网络稳定时可继续降低。
- `rtsp_transport`：RTSP 传输方式。`tcp` 表示强制 TCP，稳定性通常更好；`udp` 表示不强制 TCP，由 VLC 使用默认传输方式。

上述配置在 `InitSdk` 时读取，修改后需要重新初始化 DLL 或重启第三方程序才会生效。

### 1.2 日志责任边界

DLL 日志默认写入运行目录下的 `HZCYKJTHardWare_Logs`。日志按责任边界划分：

- `第三方调用`：表示第三方程序已经调用 DLL 导出函数，例如 `RequestNfcCard`、`SwitchTerminal`、`StartCameraPreview`。
- `向终端提交...`：表示 DLL 已经开始向当前终端发送 HTTP 请求。
- `终端已受理...`：表示终端 HTTP 接口已返回 2xx，说明终端接受了异步任务，但不代表最终业务结果成功。
- `收到终端...回调`：表示终端通过 `callback_url` 回调 DLL，返回最终异步结果。
- `已通知第三方回调`：表示 DLL 已经把事件回调给第三方注册的回调函数。

正常 `info` 日志只记录上述关键边界和结果摘要；请求体、原始 JSON、RTSP/VLC 细节放在 `debug` 日志中，便于现场排查时打开。

## 2. 核心调用模型

第三方同一时间只操作一个终端。需要切换终端时，只调用：

```c
HZCYKJTHardWare_SwitchTerminal(terminalIndex);
```

切换成功后，后续所有预览、抓拍、OCR、IC 卡识别、流程控制接口都基于当前终端执行，原接口函数名、参数和返回值不变，不需要额外传 `terminalId`。

如果切换终端时已经有预览视频在播放，DLL 会自动：

1. 记录当前预览窗口句柄 `HWND`
2. 停止旧终端预览
3. 切换内部当前终端上下文
4. 向新终端重新请求预览地址
5. 使用原来的 `HWND` 自动启动新终端预览

第三方不需要再次调用 `StartCameraPreview`、`StartFingerprintPreview` 或 `StartIrisPreview`。

`HZCYKJTHardWare_SwitchTerminal` 返回值：

- `1`：终端切换成功；如果切换前有预览，预览也已自动切换成功
- `0`：终端切换失败，或切换后自动恢复预览失败

## 3. 推荐调用流程

```c
HZCYKJTHardWare_InitSdk();
HZCYKJTHardWare_RegisterEventCallback(OnEvent, userData);

HZCYKJTHardWare_StartProcess();

HZCYKJTHardWare_SwitchTerminal(1);
HZCYKJTHardWare_StartCameraPreview(hwndCamera);
HZCYKJTHardWare_CaptureCameraImage(NULL);
HZCYKJTHardWare_RequestOCR(NULL);
HZCYKJTHardWare_RequestNfcCard(NULL);

HZCYKJTHardWare_SwitchTerminal(2);
// 如果摄像头预览正在播放，DLL 会自动把 hwndCamera 中的视频切到终端 2。
// 后续接口仍然原样调用。
HZCYKJTHardWare_CaptureCameraImage(NULL);
HZCYKJTHardWare_RequestOCR(NULL);
HZCYKJTHardWare_RequestNfcCard(NULL);

HZCYKJTHardWare_StopCameraPreview();

HZCYKJTHardWare_EndProcess();
HZCYKJTHardWare_ReleaseSdk();
```

说明：

- `InitSdk` 必须先调用。
- `RegisterEventCallback` 建议调用；使用预览状态、虹膜抓拍、OCR 结果、IC 卡识别结果时必须调用。
- `StartProcess` / `EndProcess` 是业务流程的开始和结束。
- `ReleaseSdk` 用于释放 DLL 内部资源。

## 4. 事件回调

### 4.1 回调函数签名

```c
typedef void (__stdcall *THZCYKJTHardWareEventCallback)(const char* eventJson);
```

回调接收一个 UTF-8 编码的 JSON 字符串。回调在 DLL 内部工作线程中执行，字符串指针仅在回调函数执行期间有效，第三方如需保存数据应在回调内自行复制。

### 4.2 回调 JSON 格式

```json
{
  "event_type": 2001,
  "request_id": "HZCYKJTHardWare_AUTH_...",
  "resource_type": "authorization",
  "status": 1,
  "error_code": "",
  "message": "同意授权",
  "terminal_index": 1,
  "terminal_base_url": "http://192.168.20.30:9098",
  "save_path": ""
}
```

### 4.3 各事件类型额外字段

| 事件号 | 资源类型 | 额外字段 |
|--------|----------|----------|
| `1601` | `ocr_document` | `mrz`：机读码，格式 `MRZ1^MRZ2^MRZ3` |
| `1806` | `nfc_card` | `ic_number`：IC 卡号 |
| `2001`/`2002` | `authorization` | `auth_result`：`"1"`同意/`"0"`不同意；`ZJHM`、`ZJLB`、`GJDQDM`、`XM`、`XB`、`CSRQ`、`KADM` |

### 4.4 常用事件类型

| 事件号 | 含义 |
| --- | --- |
| `1003` | 终端已切换 |
| `1201` | 摄像头预览已开始 |
| `1202` | 摄像头预览已停止 |
| `1203` | 摄像头预览失败 |
| `1301` | 指纹预览已开始 |
| `1302` | 指纹预览已停止 |
| `1303` | 指纹预览失败 |
| `1601` | OCR 成功 |
| `1602` | OCR 失败 |
| `1801` | 虹膜预览已开始 |
| `1802` | 虹膜预览已停止 |
| `1803` | 虹膜预览失败 |
| `1804` | 虹膜抓拍成功 |
| `1805` | 虹膜抓拍失败 |
| `1806` | IC 卡识别成功 |
| `1807` | IC 卡识别失败 |
| `1901` | 车牌预览已开始 |
| `1902` | 车牌预览已停止 |
| `1903` | 车牌预览失败 |
| `1999` | 通用错误 |
| `2001` | 授权同意 |
| `2002` | 授权不同意 |

## 5. 导出函数

### 5.1 初始化 DLL

```c
int __stdcall HZCYKJTHardWare_InitSdk(void);
```

初始化 DLL，读取配置文件，初始化网络、终端、回调服务、日志等内部资源。成功返回 `1`，失败返回 `0`。

### 5.2 释放 DLL

```c
int __stdcall HZCYKJTHardWare_ReleaseSdk(void);
```

释放 DLL 内部资源，停止预览、停止回调服务并清理运行状态。成功返回 `1`，失败返回 `0`。

### 5.3 注册事件回调

```c
int __stdcall HZCYKJTHardWare_RegisterEventCallback(
    THZCYKJTHardWareEventCallback callback
);
```

注册统一事件回调。成功返回 `1`，失败返回 `0`。

使用以下功能时必须注册回调：

- 预览状态通知
- 虹膜抓拍结果
- OCR 结果
- IC 卡识别结果
- 授权结果

### 5.4 开始流程

```c
int __stdcall HZCYKJTHardWare_StartProcess(void);
```

开始一次业务流程。成功返回 `1`，失败返回 `0`。

### 5.5 结束流程

```c
int __stdcall HZCYKJTHardWare_EndProcess(void);
```

结束当前业务流程，并取消未完成的异步请求。成功返回 `1`，失败返回 `0`。

### 5.6 切换终端

```c
int __stdcall HZCYKJTHardWare_SwitchTerminal(int terminalIndex);
```

切换当前终端。

参数：

- `terminalIndex`：终端序号，目前按配置支持 `1`、`2`

行为：

- 切换后，后续预览、抓拍、OCR、IC 卡识别等接口自动使用新终端。
- 第三方不需要在业务接口中传 `terminalId`。
- 如果切换前已有预览在播放，DLL 会自动把当前预览窗口切换到新终端的视频。

返回值：

- `1`：切换成功；如有预览，预览自动切换也成功
- `0`：切换失败，或预览自动切换失败

### 5.7 摄像头预览

```c
int __stdcall HZCYKJTHardWare_StartCameraPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopCameraPreview(void);
```

`hwnd` 为第三方提供的预览窗口句柄。成功返回 `1`，失败返回 `0`。

调用 `StopCameraPreview` 后，DLL 会清除保存的摄像头预览句柄。之后再切换终端时，不会自动恢复摄像头预览，除非第三方重新调用 `StartCameraPreview`。

### 5.8 指纹预览

```c
int __stdcall HZCYKJTHardWare_StartFingerprintPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopFingerprintPreview(void);
```

`hwnd` 为第三方提供的预览窗口句柄。成功返回 `1`，失败返回 `0`。

### 5.9 虹膜预览

```c
int __stdcall HZCYKJTHardWare_StartIrisPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopIrisPreview(void);
```

`hwnd` 为第三方提供的预览窗口句柄。成功返回 `1`，失败返回 `0`。

### 5.10 车牌预览

```c
int __stdcall HZCYKJTHardWare_StartPlatePreviewCJ(void* hwnd);
int __stdcall HZCYKJTHardWare_StopPlatePreviewCJ(void);
int __stdcall HZCYKJTHardWare_StartPlatePreviewRJ2(void* hwnd);
int __stdcall HZCYKJTHardWare_StopPlatePreviewRJ2(void);
int __stdcall HZCYKJTHardWare_StartPlatePreviewRJ3(void* hwnd);
int __stdcall HZCYKJTHardWare_StopPlatePreviewRJ3(void);
```

`hwnd` 为第三方提供的预览窗口句柄。成功返回 `1`，失败返回 `0`。

`hwnd` 应为第三方专门用于车牌视频显示的稳定子控件句柄。DLL 将请求转发给 C# Proxy，车牌 libVLC 仅在 Proxy 进程中运行，并直接把视频输出绑定到该 HWND；第三方程序不需要加载或释放 VLC。

预览期间第三方不要销毁、重建对应控件句柄，也不要在该控件客户区持续执行 GDI 重绘；关闭窗口前先调用对应的 `StopPlatePreviewCJ`、`StopPlatePreviewRJ2` 或 `StopPlatePreviewRJ3`。

车牌镜头属于通道级资源，不随终端1/2切换。Proxy 不处理方向组合：方向 1 由调用方调用 `StartPlatePreviewCJ`；方向 2 由调用方分别向两个独立 HWND 调用 `StartPlatePreviewRJ2` 和 `StartPlatePreviewRJ3`。CJ、RJ2、RJ3 会话相互隔离，可独立启停。

### 5.11 摄像头抓拍

```c
int __stdcall HZCYKJTHardWare_CaptureCameraImage(const char* saveDir);
```

同步抓拍人脸图片。

- `saveDir` 可传 `NULL`，表示使用配置文件中的默认保存目录。
- 成功返回 `1`，失败返回 `0`。
- 成功时 DLL 会保存抓拍图片。

### 5.12 指纹抓拍

```c
int __stdcall HZCYKJTHardWare_CaptureFingerprintImage(const char* saveDir);
```

同步抓拍指纹图片。

- `saveDir` 可传 `NULL`。
- 成功返回 `1`，失败返回 `0`。
- 成功时 DLL 会保存指纹图片。

### 5.13 虹膜抓拍

```c
int __stdcall HZCYKJTHardWare_CaptureIrisImage(const char* saveDir);
```

异步抓拍虹膜图片。

- `saveDir` 可传 `NULL`。
- 函数返回 `1` 表示请求已提交。
- 最终结果通过 `1804` 或 `1805` 事件回调返回。
- 成功时 `save_path` 返回本次请求的保存目录。

### 5.14 OCR

```c
int __stdcall HZCYKJTHardWare_RequestOCR(const char* saveDir);
```

异步发起 OCR 识别。

- `saveDir` 可传 `NULL`。
- 函数返回 `1` 表示请求已提交。
- 最终结果通过 `1601` 或 `1602` 事件回调返回。
- 成功时 DLL 会保存 OCR 原始 JSON、相关图片和解析结果，`save_path` 返回保存目录。
- 成功时 `mrz` 字段包含证件机读码信息，格式为 `MRZ1^MRZ2^MRZ3`。

### 5.15 IC 卡识别

```c
int __stdcall HZCYKJTHardWare_RequestNfcCard(const char* saveDir);
```

异步发起 IC 卡识别。

- `saveDir` 当前可传 `NULL`。
- 函数返回 `1` 表示请求已提交。
- 最终结果通过 `1806` 或 `1807` 事件回调返回。
- 成功时，DLL 通过回调结构的 `ic_number` 字段返回终端回调 JSON 中的 `data.card_text`。
- 失败时，DLL 通过回调结构的 `message` 字段返回错误说明。
- 第三方如果需要保存识别结果，应在回调函数内立即复制 `eventData->ic_number`。

### 5.16 授权请求

```c
int __stdcall HZCYKJTHardWare_RequestAuthorize(
    const char* ZJHM,
    const char* ZJLB,
    const char* GJDQDM,
    const char* XM,
    const char* XB,
    const char* CSRQ,
    const char* KADM
);
```

异步发起授权请求。

参数：

- `ZJHM`：证件号码
- `ZJLB`：证件类别
- `GJDQDM`：国家地区代码
- `XM`：姓名
- `XB`：性别
- `CSRQ`：出生日期
- `KADM`：口岸代码

函数返回 `1` 表示请求已受理，返回 `0` 表示受理失败。

最终结果通过 `2001`（授权同意）或 `2002`（授权不同意）事件回调返回。回调 JSON 额外包含以下字段：

- `auth_result`：授权结果，`1` 同意，`0` 不同意
- `ZJHM`、`ZJLB`、`GJDQDM`、`XM`、`XB`、`CSRQ`、`KADM`：与请求参数对应

> 当前为模拟接口，固定返回授权同意结果。

## 6. C/C++ 声明示例

```c
typedef void (__stdcall *THZCYKJTHardWareEventCallback)(const char* eventJson);

extern "C" {
int __stdcall HZCYKJTHardWare_InitSdk(void);
int __stdcall HZCYKJTHardWare_ReleaseSdk(void);
int __stdcall HZCYKJTHardWare_RegisterEventCallback(
    THZCYKJTHardWareEventCallback callback
);

int __stdcall HZCYKJTHardWare_StartProcess(void);
int __stdcall HZCYKJTHardWare_EndProcess(void);
int __stdcall HZCYKJTHardWare_SwitchTerminal(int terminalIndex);

int __stdcall HZCYKJTHardWare_StartCameraPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopCameraPreview(void);
int __stdcall HZCYKJTHardWare_StartFingerprintPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopFingerprintPreview(void);
int __stdcall HZCYKJTHardWare_StartIrisPreview(void* hwnd);
int __stdcall HZCYKJTHardWare_StopIrisPreview(void);
int __stdcall HZCYKJTHardWare_StartPlatePreviewCJ(void* hwnd);
int __stdcall HZCYKJTHardWare_StopPlatePreviewCJ(void);
int __stdcall HZCYKJTHardWare_StartPlatePreviewRJ2(void* hwnd);
int __stdcall HZCYKJTHardWare_StopPlatePreviewRJ2(void);
int __stdcall HZCYKJTHardWare_StartPlatePreviewRJ3(void* hwnd);
int __stdcall HZCYKJTHardWare_StopPlatePreviewRJ3(void);

int __stdcall HZCYKJTHardWare_CaptureCameraImage(const char* saveDir);
int __stdcall HZCYKJTHardWare_CaptureFingerprintImage(const char* saveDir);
int __stdcall HZCYKJTHardWare_CaptureIrisImage(const char* saveDir);
int __stdcall HZCYKJTHardWare_RequestOCR(const char* saveDir);
int __stdcall HZCYKJTHardWare_RequestNfcCard(const char* saveDir);

int __stdcall HZCYKJTHardWare_RequestAuthorize(
    const char* ZJHM, const char* ZJLB, const char* GJDQDM,
    const char* XM, const char* XB, const char* CSRQ, const char* KADM
);
}
```

## 7. Delphi 7 声明示例

```pascal
const
  HZCYKJTHardWare_DLL = 'HZCYKJTHardWare.dll';

type
  THZCYKJTHardWareEventCallback = procedure(EventJson: PAnsiChar); stdcall;

function HZCYKJTHardWare_InitSdk: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_ReleaseSdk: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_RegisterEventCallback(Callback: THZCYKJTHardWareEventCallback): Integer; stdcall; external HZCYKJTHardWare_DLL;

function HZCYKJTHardWare_StartProcess: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_EndProcess: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_SwitchTerminal(TerminalIndex: Integer): Integer; stdcall; external HZCYKJTHardWare_DLL;

function HZCYKJTHardWare_StartCameraPreview(hwnd: Pointer): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StopCameraPreview: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StartFingerprintPreview(hwnd: Pointer): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StopFingerprintPreview: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StartIrisPreview(hwnd: Pointer): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StopIrisPreview: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StartPlatePreviewCJ(hwnd: Pointer): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StopPlatePreviewCJ: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StartPlatePreviewRJ2(hwnd: Pointer): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StopPlatePreviewRJ2: Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StartPlatePreviewRJ3(hwnd: Pointer): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_StopPlatePreviewRJ3: Integer; stdcall; external HZCYKJTHardWare_DLL;

function HZCYKJTHardWare_CaptureCameraImage(SaveDir: PAnsiChar): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_CaptureFingerprintImage(SaveDir: PAnsiChar): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_CaptureIrisImage(SaveDir: PAnsiChar): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_RequestOCR(SaveDir: PAnsiChar): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_RequestNfcCard(SaveDir: PAnsiChar): Integer; stdcall; external HZCYKJTHardWare_DLL;
function HZCYKJTHardWare_RequestAuthorize(ZJHM, ZJLB, GJDQDM, XM, XB, CSRQ, KADM: PAnsiChar): Integer; stdcall; external HZCYKJTHardWare_DLL;
```

Delphi 回调接收到 JSON 字符串后，解析示例：

```pascal
procedure EventCallback(EventJson: PAnsiChar); stdcall;
var
  Json: string;
begin
  Json := Utf8ToAnsi(EventJson);  // 转为 ANSI 字符串后解析
  // 从 Json 中提取 event_type、resource_type、message 等字段
end;
```
